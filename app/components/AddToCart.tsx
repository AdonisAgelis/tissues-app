"use client";

export default function AddToCart() {
  return (
    <button
      className="btn btn-primary"
      onClick={() => console.log("Add to Cart")}
    >
      Add to Cart
    </button>
  );
}
